# 🚀 Швидкий старт AI Workflow Automation System

## 📋 Перед початком

Переконайтеся, що у вас є:
- Python 3.9+
- Telegram Bot Token (від @BotFather)
- OpenAI API Key (GPT-4.1)
- Anthropic API Key (Claude Sonnet 4)

## ⚡ Швидке налаштування

### 1. Встановлення залежностей
```bash
pip3 install -r requirements.txt --break-system-packages
```

### 2. Налаштування конфігурації
```bash
# Створіть файл .env на основі прикладу
cp .env.example .env

# Відредагуйте .env файл:
nano .env
```

Заповніть ваші API ключі:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
```

### 3. Запуск бота
```bash
python3 bot.py
```

## 🎯 Тестування системи

### Використайте тестовий файл
1. Запустіть бота
2. Надішліть `/start` у Telegram
3. Завантажте файл `test_story.txt`
4. Слідуйте інструкціям бота

### Workflow тестування:
1. **Файл** → Завантажте `test_story.txt`
2. **Промт GPT** → Пропустіть або додайте: "Зробити більш детальним"
3. **Outline** → Підтвердьте згенерований план
4. **Промт Sonnet** → Пропустіть або додайте: "Додати емоцій"
5. **Обсяг** → Оберіть 15K для швидшого тесту або 30K для стандартного
6. **Режим** → Оберіть стандартний
7. **Результат** → Дочекайтеся автоматичної генерації

## 📁 Результати

Після успішної генерації у папці `Проект/` з'являться:
- `Work/` - файли з валідними відповідями (#WORK)
- `Not valid/` - файли поза цільовим діапазоном (#notvalid)

## 🔧 Команди для тестування

```bash
# Перевірка статусу
/status

# Скасування операції
/cancel

# Довідка
/help

# Новий старт
/start
```

## 🚨 Можливі проблеми

### Помилка "Module not found"
```bash
pip3 install -r requirements.txt --break-system-packages
```

### Помилка API ключів
- Перевірте правильність ключів у файлі `.env`
- Переконайтеся, що у вас є кредити на OpenAI та Anthropic

### Бот не відповідає
- Перевірте Telegram Bot Token
- Переконайтеся, що бот запущений

## 📊 Очікувані результати

### Для 15K режиму:
- Цільовий діапазон: 15,000 - 20,000 символів
- Найшвидший для тестування
- Файли автоматично класифікуються

### Для 30K режиму:
- Цільовий діапазон: 28,000 - 34,000 символів
- Зазвичай потрібно 1-5 спроб
- Стандартний режим

### Для 40K режиму:
- Цільовий діапазон: 40,000 - 50,000 символів
- Середній обсяг контенту
- Збалансований варіант

### Для 60K режиму:
- Цільовий діапазон: 56,000 - 68,000 символів
- Може потребувати більше спроб
- Більш детальний контент

## 🎉 Успішний тест

Якщо все працює правильно, ви отримаєте:
- ✅ Файл у папці `Work/` з тегом #WORK
- 📊 Детальну статистику генерації
- 📁 Організовані файли з метаданими
- 🎯 Контент у цільовому діапазоні символів

---

**Готово!** Ваша AI Workflow Automation System працює та готова до використання! 🚀